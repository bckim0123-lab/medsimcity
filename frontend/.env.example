# ── 로컬 개발 시 (.env.local 파일로 복사하여 사용) ───────────────
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Vercel 배포 시 ───────────────────────────────────────────────
# NEXT_PUBLIC_API_URL 은 설정하지 않아도 됩니다 (자동 프록시 사용).
# 대신 아래 서버사이드 환경변수를 설정하세요:
#
# BACKEND_URL=https://medsimcity.onrender.com
