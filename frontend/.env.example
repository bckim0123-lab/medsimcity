# Vercel 배포 시 환경변수로 설정 (대시보드 또는 vercel env add)
# 로컬 개발 시에는 .env.local 파일로 복사하여 사용

# Backend API 주소 (Render 배포 후 발급되는 URL로 교체)
NEXT_PUBLIC_API_URL=https://medisim-api.onrender.com

# 로컬 개발 시
# NEXT_PUBLIC_API_URL=http://localhost:8000
